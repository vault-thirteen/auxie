package muc

import (
	"testing"
)

func Test_CheckFreeMemory(t *testing.T) {

	// Notes.
	// The 'CheckFreeMemory' Method can not be tested while it depends on a
	// Method which can not be truly tested.
}
